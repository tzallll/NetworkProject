# NetworkProject
Author:Nikolaos Tzallas
AEM:4728
#Εργασία Client-Server Handler
Το σύστημα χρησιμοποιεί ένα Request-Reply πρωτόκολλο, όπου οι clients στέλνουν αιτήσεις (requests) στον server και ο server απαντά με αποτελέσματα (responses). 
Με αυτόν τον τρόπο επιτυγχάνεται η επικοινωνία μεταξύ πολλών χρηστών. 

#Χρησιμοποιούμενες Τεχνολογίες
Java Sockets (TCP) για την επικοινωνία client-server.
I/O Streams (BufferedReader και PrintWriter) για την ανάγνωση και αποστολή μηνυμάτων.
Threads για την ταυτόχρονη εξυπηρέτηση πολλαπλών clients.
Δομές δεδομένων όπως Map και ArrayList για την αποθήκευση λογαριασμών και μηνυμάτων.

#ΚΛΑΣΕΙΣ
#Account
Ο server διατηρεί όλους τους λογαριασμούς σε μια λίστα accounts.

#Message
Χρησιμοποιείται για αποθήκευση και προβολή μηνυμάτων στον server.

#Server
Κάθε σύνδεση εκχωρείται σε δικό της Thread.
Create Account
Show Accounts
Send Message
Show Inbox
Read Message
Delete Message
Χρησιμοποιεί authToken για έλεγχο πρόσβασης.

#Client
Στέλνει αιτήματα στον server.
Παίρνει input από τον χρήστη και εμφανίζει τα αποτελέσματα.
Καλεί τις λειτουργίες του server μέσω FN_ID και παραμέτρων.

Παραπάνω πληροφορίες στα σχόλια του κώδικα

Για λόγους καλής παρουσίασης των δεδομένων(εισαγωγή/εκτύπωση), υπάρχει μενού διαχείρισης
από τον πελάτη, μηνυμάτα εισαγωγής(π.χ.token) με newline,επιπλέον μηνύματα λάθους(π.χ Missing Username),μηνύματα 
τερματισμού(π.χ.ΕΝD) και μηνύματα εκτύπωσης όχι όλα σε σειρά,αλλά το ένα κάτω από το άλλο.

Επιπλέον για λόγους καλύτερης απόδοσης χρησιμοποιήθηκαν από τα νήματα το pooling,
flush και atomic integers(βλ.σχόλια κώδικα για το πως αξιοποιήθηκαν).

Aκόμα για λόγους διαχείρισης η συμβολοσειρά απαίτησης του πελάτη χωρίστηκε σε μέρη με τη
μέθοδο split(για όποια μέρη χρειάστηκε μετατροπή σε ακέραιο έγινε με την μέθοδο ParseInt).

Για μετατροπή αναγνωριστικού σε ακέραιο και απευθείας αντιστοίχιση με χρήστη χρησιμοποιήθηκε η μέθοδος getUserFromToken
στην κλάση του server.

Eπιπρόσθετες λειτουργίες δεν υλοποιήθηκαν

Ο Server τρέχει συνεχώς.Τηρείται ο ταυτοχρονισμός.Κατάλληλη σειριοποιήση δεδομένων.

